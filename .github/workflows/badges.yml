name: Badges Update

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  update-badges:
    name: Update Repository Badges
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Create badge data
        run: |
          echo "Badges are automatically generated by workflows and displayed in README"
