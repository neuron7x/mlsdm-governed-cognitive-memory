# MLSDM Full Installation Requirements
#
# This file includes ALL dependencies including optional ones.
# For minimal installation: pip install -e .
# For embeddings support: pip install -e ".[embeddings]"
# For full dev install: pip install -r requirements.txt
#
# Core Dependencies (sync with pyproject.toml [project.dependencies])
numpy>=2.0.0
fastapi>=0.115.0
uvicorn>=0.32.0
pyyaml>=6.0.2
pydantic>=2.10.0
prometheus-client>=0.21.0
tenacity>=9.0.0
requests>=2.32.3
psutil>=6.1.0
defusedxml>=0.7.1

# Optional Embeddings (from pyproject.toml [project.optional-dependencies].embeddings)
# Install with: pip install ".[embeddings]" when semantic embeddings are needed
sentence-transformers>=3.0.0

# Optional Observability (from pyproject.toml [project.optional-dependencies].observability)
# Install with: pip install ".[observability]" for distributed tracing
opentelemetry-api>=1.31.0
opentelemetry-sdk>=1.31.0
pyjwt>=2.10.0

# Dev dependencies (from pyproject.toml [project.optional-dependencies].dev)
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
hypothesis>=6.122.0
ruff>=0.9.0
mypy>=1.15.0
deptry>=0.24.0
locust>=2.32.0
httpx>=0.28.0
types-PyYAML>=6.0.12
types-requests>=2.32.0

# Visualization (optional, from pyproject.toml [project.optional-dependencies].visualization)
matplotlib>=3.8.4

# Security: Pin minimum versions for indirect dependencies with known vulnerabilities
certifi>=2025.11.12
cryptography>=46.0.3
jinja2>=3.1.6
urllib3>=2.6.2
setuptools>=80.9.0
idna>=3.11
pip>=25.3
